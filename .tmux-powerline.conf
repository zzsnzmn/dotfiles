if-shell 'test -z "$POWERLINE_COMMAND"' 'if-shell "which powerline-client" "set-environment -g POWERLINE_COMMAND powerline-client" "set-environment -g POWERLINE_COMMAND powerline"'

SESSION="brightwhite"
STATUSBG="white"
STATUSFG="blue"


set -g status on
set -g status-utf8 on
set -g status-interval 2
set -g status-fg $STATUSFG
set -g status-bg $STATUSBG
set -g status-left-length 20
set -g status-left "#[fg=$STATUSFG,bg=$SESSION,bold] #S #[fg=$SESSION,bg=$STATUSBG,nobold]#(eval $POWERLINE_COMMAND tmux left)"
set -g status-right '#(eval $POWERLINE_COMMAND tmux right -R pane_id=`tmux display -p "#D"`)'
set -g status-right-length 150
set -g window-status-format "#[fg=$STATUSFG,bg=$STATUSBG]#I #[fg=$STATUSFG] #[fg=$STATUSFG]#W "
set -g window-status-current-format "#[fg=$STATUSBG,bg=$STATUSFG]#[fg=$STATUSBG,bg=$STATUSFG] #I  #[fg=$STATUSBG,bold]#W #[fg=$STATUSFG,bg=$STATUSBG,nobold]"


set-window-option -g window-status-fg $STATUSBG
set-window-option -g window-status-activity-attr none
set-window-option -g window-status-bell-attr none
set-window-option -g window-status-activity-fg yellow
set-window-option -g window-status-bell-fg red
# vim: ft=tmux
